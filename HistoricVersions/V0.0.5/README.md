# Velaris V0.0.5

Frozen historic release of Velaris V0.0.5.

The repository state before the V0.0.6 transition is preserved by commit `203f92462bb3fc8e2b8b50d8daab3f63663df849`.

This historic folder keeps pinned loaders for the V0.0.5 CSS and JavaScript so the release remains reproducible even after `main` moves forward.

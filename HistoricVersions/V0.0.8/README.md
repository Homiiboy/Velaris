# Velaris V0.0.8

Frozen historic release of Velaris V0.0.8 — Settings & Branding.

This snapshot resolves its active CSS and JavaScript from the immutable GitHub release tag `v0.0.8` so the release remains reproducible after later development.
